
/**
 * ArduProgram IDE - Local Mode
 * IA desativada conforme solicitação do usuário.
 */
export const getCodeAssistance = async (prompt: string, currentCode: string) => {
  return "Funcionalidade de IA desativada.";
};

export const analyzeCode = async (code: string) => {
  return { status: "Ok", summary: "Análise local básica concluída." };
};
